[metadata]
name = pkg-setup-cfg
version = 0.1.0
description = A minimal Hello World test package for colcon build with setup.cfg

[options]
packages = find:
python_requires = >=3.6

[options.entry_points]
console_scripts =
    hello-cfg = pkg_setup_cfg.main:main
